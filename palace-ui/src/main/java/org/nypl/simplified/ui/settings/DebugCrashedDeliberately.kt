package org.nypl.simplified.ui.settings

/**
 * An exception to unique identify instances where the app was crashed deliberately using the
 * button in the debug menu.
 */

class DebugCrashedDeliberately : Exception()
